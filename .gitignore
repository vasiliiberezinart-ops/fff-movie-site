# Big raw masters — не пушить (хранятся локально)
assets/raw/*-master.*
assets/raw/*-original.*
assets/raw/*.mov
assets/raw/*-wip.mp4
assets/raw/*-fullfilm.mp4

# Веб-готовые тизеры в репо ОСТАВЛЯЕМ (нужны для сайта):
# assets/raw/*-teaser.mp4 — да
# assets/raw/hero-trailer.mp4 — да

# Кадры на выбор постера — рабочий материал
assets/frames-pick/

# Превью-страницы для рабочего обсуждения
preview-palettes.html
preview-frames.html
preview-frames-igt.html

# OS / IDE
.DS_Store
*.swp
.idea/
.vscode/

# Node / build
node_modules/
dist/
.cache/

# Font pipeline intermediates (TTF is in assets/fonts; .py scripts kept)
font-build/glyphs_pgm/
font-build/glyphs_svg/
font-build/glyphs_png/
font-build/debug/
